import serial
import hibike_message as hm

# create serial object (/dev//ttyACM0, 115200)
#
# start = time.time()
# hm.send(object, hm.make_device_update(1, 20))
# while hw.read(object) in (None, 1):
#     pass
# end = time.time()
